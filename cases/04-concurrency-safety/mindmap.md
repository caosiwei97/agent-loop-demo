# 并发安全调度
## 工具安全分类
### Read / Grep → 只读，可并发
### Edit / Write → 修改状态，必须串行
### Bash → 看具体命令判断
## 并发策略
### Promise.all 并发执行读操作
### 写操作必须逐个串行
### 结果按调用顺序返回（非完成顺序）
## 级联规则
### Bash 失败 → 兄弟 Bash 被取消
### Bash 失败 → Read 类不受影响
### 只有 Bash 的错误会级联
## 核心原则
### 能并发的尽量并发
### 不能并发的坚决串行
### 安全和性能之间找平衡
